---
const baseUrl = import.meta.env.BASE_URL;
const normalizedBaseUrl = baseUrl.endsWith('/') ? baseUrl : baseUrl + '/';
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href={`${normalizedBaseUrl}favicon.svg`} />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Astro</title>
	</head>
	<body>
		<h1>Astroだよねー</h1>
		
		<!-- 正しい画像パスの例 -->
		<img src={`${normalizedBaseUrl}images/test.jpg`} alt="サンプル画像" />
		
		<!-- 他の静的ファイルも同様 -->
		<p>このパスは開発時も本番時も正しく動作します</p>
		
		<p><strong>Base URL (raw):</strong> {baseUrl}</p>
		<p><strong>Base URL (normalized):</strong> {normalizedBaseUrl}</p>
		<p><strong>Image URL:</strong> {normalizedBaseUrl}images/test.jpg</p>
	</body>
</html>
